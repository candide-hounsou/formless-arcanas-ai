[project]
name = "formless-arcanas-ai"
version = "0.1.0"
description = "Liability-aware financial decision engine (reference project) built with Pydantic AI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.7",
  "pydantic-ai>=0.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
